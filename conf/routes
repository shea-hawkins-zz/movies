# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# A
GET     /star_wars/movies                         star_wars.StarWarsController.getMoviesByDirector
# An example controller showing how to use dependency injection
GET     /star_wars/movies/:movie_id/characters    star_wars.StarWarsController.getCharactersForMovie(movie_id: String)
